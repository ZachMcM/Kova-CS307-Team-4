import {TaggedSearchItem, WordCounter, TagCounter, sortTaggedList} from "@/types/searcher-types"
import { Component } from "react";

export default function TaggedSearchView(items: TaggedSearchItem[], callback: (id: string) => void, templateGetter: (id: string) => Component) {

}